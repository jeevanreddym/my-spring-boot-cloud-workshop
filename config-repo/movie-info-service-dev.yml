
app:
  name: My App Dev
  description: Welcome to ${spring.application.name}

my:
  greeting: Welcome to ${app.name}
  list:
    values: one,two,three
  dbValues: "{url: 'http://oracledb/dev', username: 'dev', password: 'dev'}"

db:
  connection: "http://oracledb/dev"
  host: 127.0.0.1
  port: 1200
